apiVersion: v1
kind: Service

metadata:
  name: user-service
  namespace: staging

  labels:
    app: user-service

spec:
  type: ClusterIP

  selector:
    app: user-service

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
