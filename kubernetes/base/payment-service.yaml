apiVersion: v1
kind: Service

metadata:
  name: payment-service
  namespace: staging

  labels:
    app: payment-service

spec:
  type: ClusterIP

  selector:
    app: payment-service

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
